# Email templates

Transactional email templates rendered and delivered through the Resend client
in `emails/client.ts`. Add one file per template, keeping markup and the
associated subject/props type together.
